export const VOICE_DEVELOPER_INSTRUCTIONS =
  "Use speak.text for the complete spoken answer and read.markdown for the readable screen version.";

export function voicePrompt(prompt: string): string {
  return prompt;
}
